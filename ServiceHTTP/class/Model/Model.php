<?php

class Model{

    function __construct ( $datas ){
        $this->hydrate ( $datas );

        function hydrate ( $datas ){
            foreach( $datas as $key => $data){
                $method = "set"
            }
        }
    }
} 